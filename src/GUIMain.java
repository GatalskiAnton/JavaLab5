import javax.swing.JFrame;
import java.awt.*;

public class GUIMain {
    public static void main(String[] args) {
        MyFrame frame = new MyFrame("Series");
        frame.setSize(500, 600);
        frame.setLocationRelativeTo(null);
        frame.setVisible(true);
    }
}
